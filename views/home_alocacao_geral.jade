extends layout

block content
	h1.
		Selecione #{num_hospede} leitos
	form(class='form-signin', action='/home/' + '#{endereco}', method='POST')
		button(type="submit", name='leitos') Alocar
		ul
			each leito, j in leitos
				input(type="checkbox", name=leito) 
				Tick #{leito}
				br